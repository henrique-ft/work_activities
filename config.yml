parsed_file_path: parsed.md
title: your name
editor: gedit
activities_file_template:
  tasks:
  meetings:
  annotations:
  meeting_annotations:
  interruptions:
  difficulties:
  pomodoros:

# leave empty to get the current dir
activities_dir:
